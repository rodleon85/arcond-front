import { FormatCurrencyBrPipe } from '../format-currency-br.pipe';

describe('FormatCurrencyBrPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCurrencyBrPipe();
    expect(pipe).toBeTruthy();
  });
});
